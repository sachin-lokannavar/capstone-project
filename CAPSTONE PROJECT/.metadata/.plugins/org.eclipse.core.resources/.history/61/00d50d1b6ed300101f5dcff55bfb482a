package com.pizzastore.userservice.client;

import com.pizzastore.userservice.dto.OrderRequest;
import org.springframework.cloud.openfeign.FeignClient;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.PostMapping;
import org.springframework.web.bind.annotation.RequestBody;
import org.springframework.web.bind.annotation.RequestParam; // Import this!
import java.util.List;

@FeignClient(name = "order-service")
public interface OrderClient {

    @PostMapping("/order/orders/place")
    Object placeOrder(@RequestBody OrderRequest request);

    @GetMapping("/order/orders/user/{userId}")
    List<Object> getOrdersByUser(@PathVariable("userId") Long userId);

    // ✅ FIX: Use the Update endpoint to set status to CANCELLED
    // This matches the working logic in your Admin Service
    @PostMapping("/order/orders/update/{id}")
    void updateOrderStatus(@PathVariable("id") Long id, @RequestParam("status") String status);
}