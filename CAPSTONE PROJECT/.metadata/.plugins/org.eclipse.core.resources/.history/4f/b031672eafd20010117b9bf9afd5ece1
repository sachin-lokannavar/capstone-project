<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
    <title>Our Menu</title>
    <link rel="stylesheet" th:href="@{/css/style.css}">
    </head>
<body>
<div class="navbar">
    <div class="nav-logo">🍕 Pizza Store</div>
    <div class="nav-links">
        <a th:href="@{/dashboard}">Dashboard</a>
        <a th:href="@{/cart}">🛒 View Cart</a> <a th:href="@{/logout}">Logout</a>
        <a href="/user/my-orders" style="margin-right: 15px; color: white; text-decoration: none;">My Orders</a>
    </div>
</div>

<div class="container" style="width: 90%;">
    <h2>Our Delicious Menu</h2>
    
    <div class="menu-grid">
        <div class="card" th:each="item : ${menuItems}" th:if="${item.available}">
            <img th:if="${item.imageUrl != null}" th:src="${item.imageUrl}" alt="Pizza" width="150" height="150">
            <h3 th:text="${item.name}">Pizza</h3>
            <p th:text="${item.description}"></p>
            <p style="font-weight:bold; color:green;" th:text="'$' + ${item.price}"></p>
            
            <a th:href="@{/cart/add/{id}(id=${item.id}, name=${item.name}, price=${item.price})}" 
               class="btn-buy" 
               style="background:#ff9800; color:white; padding:10px; text-decoration:none; border-radius:5px;">
               Add to Cart
            </a>
        </div>
    </div>
</div>
</body>
</html>